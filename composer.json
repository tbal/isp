{
    "name": "tiloballer/isp",
    "type": "library",
    "license": "WTFPL",
    "authors": [
        {
            "name": "Tilo Baller",
            "email": "tilo.baller@gmail.com"
        }
    ],
    "require": {
        "vlucas/phpdotenv": "^2.2"
    },
    "autoload": {
        "psr-4": {
            "TiloBaller\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "[ -e .env ] || cp .env.example .env"
        ],
        "post-update-cmd": [
            "[ -e .env ] || cp .env.example .env"
        ]
    }
}
